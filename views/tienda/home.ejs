<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenido a Joinnus</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/store/home.css">
</head>
<body class="joinnus-body">
    <%- include('../includes/headerStore.ejs') %>

    <main>
        <div class="principal-banner">
            <img src="https://cdn.joinnus.com/files/2024/10/FCgwZSar8NTTz7I.png" alt="banner-img">
        </div>

        <div class="principal-events">
            <h3 class="subtitle"> Eventos destacados </h3>
                <% if (ev.length > 0) { %>
                    <div class="eventos-destacados">
                        <% for (let evento of ev.slice(0, 8)) { %>
                            <div class="evento">
                                <img src="<%=evento.urlImagen %>" alt="Evento">
                                <div class="info-evento">
                                    <h3 class="title-event"><%= evento.nombre %></h3>
                                    <p>Fecha: <%= evento.fecha %></p>
                                    <p class="lugar"> <%= evento.lugar %> </p>
                                    <a href="/tienda/detalle-evento/<%= evento.codigo %>" class="btn-comprar"> Comprar </a>                    
                                </div>
                            </div>
                        <% } %>
                    </div>
                <% } else { %>
                    <h1>No hay eventos para mostrar</h1>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>